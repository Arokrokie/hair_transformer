### requirements.cpu.txt
# CPU-friendly requirements for Render / small hosts.
# NOTE: Pre-install a CPU PyTorch wheel (see README) before running
# `pip install -r requirements.cpu.txt` to avoid pip pulling CUDA wheels.

# Web / Django runtime
Django==4.2.7
gunicorn==20.1.0
whitenoise==6.5.0
django-crispy-forms==2.5
crispy-bootstrap5==2025.6

# Imaging & utilities
Pillow==12.0.0
numpy==2.2.6
opencv-python==4.12.0.88
imageio==2.37.0

# Lightweight ML / inference (CPU): transformers + diffusers work on CPU
transformers==4.57.2
diffusers==0.35.2
safetensors==0.7.0
accelerate==1.12.0
huggingface-hub==0.35.3

# Optional but commonly used (keep if you use these features)
scikit-image==0.25.2
scikit-learn==1.6.1
onnxruntime==1.23.2

# Networking & helpers
requests==2.32.5
python-dotenv==1.0.0

# NOTE:
# - Do NOT include GPU-only packages such as `bitsandbytes`, `triton`, or
#   `xformers` in this file. If your original `requirements.txt` had pinned
#   `torch` or `torchvision` CUDA wheels, avoid those pins; instead
#   preinstall the CPU torch wheel in your Render Build Command.
