### requirements.txt (SegFormer + Replicate only)
## Minimal stack: Django runtime + SegFormer (transformers/torch) + Replicate helpers.
## Install CPU torch/torchvision wheels before this file to avoid CUDA downloads.
## Example build command (Render):
## pip install -U pip
## pip install "torch==2.9.0+cpu" "torchvision==0.24.0+cpu" -f https://download.pytorch.org/whl/torch_stable.html
## pip install -r requirements.txt

# Core web/runtime
Django==4.2.7
gunicorn==20.1.0
whitenoise==6.5.0
django-crispy-forms==2.5
crispy-bootstrap5==2025.6

# Imaging / utilities
Pillow==12.0.0
numpy==2.2.6
opencv-python==4.12.0.88

# ML components actually used
transformers==4.57.2
safetensors==0.7.0
scikit-learn==1.6.1
torch==2.9.0+cpu
torchvision==0.24.0+cpu

# Networking / helpers
requests==2.32.5
python-dotenv==1.0.0

# Torch/torchvision pins target CPU wheels; keep the '+cpu' suffix to avoid CUDA downloads.